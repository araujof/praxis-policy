# Location: ./crates/ppe-sdk/Cargo.toml
# Copyright 2025
# SPDX-License-Identifier: Apache-2.0
# Authors: Teryl Taylor
#
# PPE SDK — lean crate for plugin authors.
# Re-exports the Plugin trait and payload/result types from praxis-policy-core
# without pulling in the PluginManager, hosts, or FFI.

[package]
name = "praxis-policy-sdk"
description = "PPE plugin author SDK — Plugin trait, payloads, and result types."
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
homepage.workspace = true
keywords.workspace = true
categories.workspace = true
rust-version.workspace = true

[dependencies]
praxis-policy-core = { workspace = true }

[lints]
workspace = true
